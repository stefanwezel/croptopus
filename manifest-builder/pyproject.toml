[project]
name = "manifest-builder"
version = "0.1.0"
description = "Croptopus manifest builder — author and round-trip the LoRa sensor-network manifest."
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "pyyaml>=6.0",
]
